namespace XmppApi.Network.XML.Messages.Processor
{
    internal enum TLSState
    {
        ERROR,
        NOT_ALLOWED,
        DISCONNECTED,
        CONNECTING,
        REQUESTED,
        CONNECTED,
        PROHIBITED
    }
}
